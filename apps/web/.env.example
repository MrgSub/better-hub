# ──────────────────────────────────────────────
# Authentication (required)
# ──────────────────────────────────────────────
# Create a GitHub OAuth App: https://github.com/settings/developers
GITHUB_CLIENT_ID=your_github_oauth_app_client_id
GITHUB_CLIENT_SECRET=your_github_oauth_app_client_secret

# Skip the real OAuth app entirely: run `npx emulate start --service github`
# and point the app at it. Sign-in then picks a local user, and every GitHub API
# call is served by the emulator. Development and CI only — a production server
# refuses to start with this set.
# The emulator has no GraphQL endpoint, so repo pages (which use it) won't load.
# GITHUB_EMULATOR_URL=http://localhost:4000

# Random 32-char string for session encryption (required)
# Generate with: openssl rand -hex 16
BETTER_AUTH_SECRET=generate_a_32_char_random_string

# Base URL of the app (required)
BETTER_AUTH_URL=http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ──────────────────────────────────────────────
# Database (required)
# ──────────────────────────────────────────────
# PostgreSQL connection string
# Default for docker compose: postgresql://postgres:postgres@localhost:54320/better_hub
DATABASE_URL=your_prisma_database_url

# ──────────────────────────────────────────────
# Redis (required)
# ──────────────────────────────────────────────
# Upstash Redis REST API for caching GitHub API data
# Default for docker compose (uses serverless-redis-http proxy):
UPSTASH_REDIS_REST_URL=http://localhost:8079
UPSTASH_REDIS_REST_TOKEN=local_token

# ──────────────────────────────────────────────
# Git backend
# ──────────────────────────────────────────────
# Which backend stores repositories. Defaults to code-storage.
# GIT_BACKEND=code-storage

# Pierre Code Storage (https://code.storage) — organisation name and the
# PKCS#8 ES256 private key used to sign short-lived repo-scoped JWTs.
# PIERRE_STORAGE_NAME=your_code_storage_org
# PIERRE_STORAGE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"

# Signs the backend's webhook deliveries: how a mirror's sync result, and a push
# that did not come through the app, reach us. Deliveries are refused without it.
# PIERRE_STORAGE_WEBHOOK_SECRET=your_webhook_secret

# Overrides (optional — derived from PIERRE_STORAGE_NAME by default)
# PIERRE_STORAGE_API_URL=https://api.code.storage
# PIERRE_STORAGE_GIT_HOST=your_code_storage_org.code.storage

# Which agent resolves merge conflicts: "model" (the app's own model, default)
# or "devin", which needs a key. Either way the proposal is verified and still
# waits for a human to merge it.
# CONFLICT_AGENT=model
# DEVIN_API_KEY=

# ──────────────────────────────────────────────
# AI — Ghost assistant (required for AI features)
# ──────────────────────────────────────────────
# OpenRouter API key — powers Ghost AI assistant
OPEN_ROUTER_API_KEY=your_open_router_api_key

# Model IDs (optional — defaults shown)
# GHOST_MODEL=moonshotai/kimi-k2.5
# GHOST_MERGE_MODEL=google/gemini-2.5-pro-preview

# Anthropic API key — used for specific AI tasks
ANTHROPIC_API_KEY=your_anthropic_api_key

# OpenAI API key (optional)
# OPENAPI_KEY=your_openai_api_key

# ──────────────────────────────────────────────
# Stripe
# ──────────────────────────────────────────────
# STRIPE_SECRET_KEY=your_stripe_secret_key
# STRIPE_WEBHOOK_SECRET=your_stripe_webhook_secret
# STRIPE_BASE_PRICE_ID=price_xxx
# STRIPE_METERED_PRICE_ID=price_xxx

# ──────────────────────────────────────────────
# E2B — code sandbox (optional)
# ──────────────────────────────────────────────
# E2B API key for sandboxed code execution
# E2B_API_KEY=your_e2b_api_key

# Custom E2B template (optional — falls back to default base image)
# Build with: cd apps/web && npx e2b template build
# E2B_TEMPLATE=your_e2b_template_id

# ──────────────────────────────────────────────
# Background jobs (optional)
# ──────────────────────────────────────────────
# Inngest for background job processing
# INNGEST_EVENT_KEY=your_inngest_event_key
# INNGEST_SIGNING_KEY=your_inngest_signing_key

# ──────────────────────────────────────────────
# Search & Memory (optional)
# ──────────────────────────────────────────────
# Mixedbread embeddings for semantic search
# MIXEDBREAD_API_KEY=your_mixedbread_api_key

# SuperMemory for AI conversation memory
# SUPER_MEMORY_API_KEY=your_super_memory_api_key

# ──────────────────────────────────────────────
# Integrations (optional)
# ──────────────────────────────────────────────
# Slack notifications
# SLACK_CLIENT_ID=your_slack_client_id
# SLACK_CLIENT_SECRET=your_slack_client_secret

# Vercel OIDC (for deployment auth)
# VERCEL_OIDC_TOKEN=your_vercel_oidc_token

# ──────────────────────────────────────────────
# Sentry (optional)
# ──────────────────────────────────────────────
# SENTRY_DSN=https://your-dsn@sentry.io/project-id
# SENTRY_AUTH_TOKEN=your_sentry_auth_token
